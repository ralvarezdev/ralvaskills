version: 2

project_name: rsk

before:
  hooks:
    - go mod tidy ./cli/...

builds:
  - id: rsk
    dir: cli
    main: .
    binary: rsk
    env:
      - CGO_ENABLED=0
      - GOWORK=off # build from cli/go.mod only, not the workspace
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X github.com/ralvarezdev/ralvaskills/cli/cmd.version={{ .Version }}
      - -X github.com/ralvarezdev/ralvaskills/cli/cmd.commit={{ .ShortCommit }}
      - -X github.com/ralvarezdev/ralvaskills/cli/cmd.buildDate={{ .Date }}

archives:
  - id: rsk
    name_template: "rsk-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md

checksum:
  name_template: "rsk-{{ .Version }}-checksums.txt"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^chore(registry):" # skip automated index commits
      - "^chore: trigger"   # skip manual CI triggers
